[package]
name = "nausilus"
version = "0.0.1"
description = "Nausilus desktop pet"
authors = ["TheCountOfSaintGermain"]
license = "MIT"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
